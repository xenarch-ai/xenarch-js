{
  "name": "@xenarch/js",
  "version": "0.1.0",
  "description": "Client-side bot detection and payment gate for publishers",
  "main": "dist/l.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "size": "gzip -c dist/l.js | wc -c"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "vitest": "^3.0.0",
    "jsdom": "^26.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "typescript": "^5.7.0"
  },
  "license": "MIT"
}
